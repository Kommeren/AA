/*! \page cycle Cycle
\section Concept
Cycle need to fulfill the interface: 
<pre>
    CycleArchetype {
        void flip(VertexType,VertexType);
        vbegin(VertexType v);
        vbegin();
        vend();
    }
</pre> 
<br>
CycleElem type is provided through paal::data_structures::CycleTraits and is detected automatically.
\section implementation Impelmentation
Library provide only one very simple implementation of cycle that is paal::data_structures::SimpleCycle.

*/
